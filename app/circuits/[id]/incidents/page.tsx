import { AppShell } from "@/components/app-shell";export default async function Incidents({params}:{params:Promise<{id:string}>}){const {id}=await params;return <AppShell title={`Circuit ${id} / Incident film`}><div className="empty"><h2>No finalized incidents</h2><p>Consensus decisions appear only after their transaction receipt is finalized. Inconclusive evidence remains a first-class safe outcome.</p></div></AppShell>}
