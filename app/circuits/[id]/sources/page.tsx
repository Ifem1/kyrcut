import { AppShell } from "@/components/app-shell";export default async function Sources({params}:{params:Promise<{id:string}>}){const {id}=await params;return <AppShell title={`Circuit ${id} / Sources`}><div className="empty"><h2>Source manifest unavailable</h2><p>Only the protocol owner may set 1–8 HTTPS allowlisted sources. Kyrcut stores normalized evidence metadata and fingerprints, never full page bodies.</p></div></AppShell>}
