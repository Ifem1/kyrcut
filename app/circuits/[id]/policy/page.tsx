import { AppShell } from "@/components/app-shell";export default async function Policy({params}:{params:Promise<{id:string}>}){const {id}=await params;return <AppShell title={`Circuit ${id} / Policy`}><div className="empty"><h2>Policy unavailable</h2><p>Policy versions lock freshness, source diversity, cooldowns, and action tiers before a heartbeat is evaluated. Changing a policy must be an owner-authorized transaction.</p></div></AppShell>}
