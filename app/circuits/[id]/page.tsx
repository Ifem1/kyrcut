import Link from "next/link";import { AppShell } from "@/components/app-shell";import { CircuitRail, CapabilitySeal, ModeBadge } from "@/components/domain";
export default async function Circuit({params}:{params:Promise<{id:string}>}){const {id}=await params;return <AppShell title={`Circuit ${id}`}><div className="detail"><div><p className="eyebrow">UNVERIFIED UNTIL READ FROM CHAIN</p><h2>Contract data unavailable</h2><p>This route requires a configured Kyrcut contract and a successful read. It does not substitute fixture data for protocol state.</p><CircuitRail/><ModeBadge mode="SHADOW"/></div><CapabilitySeal/><aside><Link href={`/circuits/${id}/sources`}>Source manifest</Link><Link href={`/circuits/${id}/policy`}>Policy & mode</Link><Link href={`/circuits/${id}/incidents`}>Incident film</Link><Link href={`/circuits/${id}/capability`}>Pause capability</Link></aside></div></AppShell>}
